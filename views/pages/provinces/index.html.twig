<!-- views/provinces/index.php -->
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Provincias</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-4">
    <h1>Provincias</h1>
    <a href="/provinces/create" class="btn btn-primary mb-3">Agregar Provincia</a>

    <table class="table table-bordered table-hover">
        <thead class="table-light">
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Código</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <?php if (!empty($provinces)): ?>
                <?php foreach ($provinces as $province): ?>
                    <tr>
                        <td><?= htmlspecialchars($province['id']) ?></td>
                        <td><?= htmlspecialchars($province['name']) ?></td>
                        <td><?= htmlspecialchars($province['code']) ?></td>
                        <td>
                            <a href="/provinces/edit?id=<?= $province['id'] ?>" class="btn btn-sm btn-warning">Editar</a>
                            <form action="/provinces/delete" method="POST" style="display:inline;" onsubmit="return confirm('¿Eliminar esta provincia?');">
                                <input type="hidden" name="id" value="<?= $province['id'] ?>">
                                <button type="submit" class="btn btn-sm btn-danger">Eliminar</button>
                            </form>
                        </td>
                    </tr>
                <?php endforeach ?>
            <?php else: ?>
                <tr><td colspan="4" class="text-center">No hay provincias registradas.</td></tr>
            <?php endif ?>
        </tbody>
    </table>
</div>
</body>
</html>
