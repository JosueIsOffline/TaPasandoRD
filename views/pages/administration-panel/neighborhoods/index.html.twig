{% extends "layout.html.twig" %}
{% block content %}


<body>
    <div class="container mt-4">
        <h1>Listado de Barrios</h1>
        <a href="/neighborhoods/create" class="btn btn-primary mb-3">Agregar nuevo barrio</a>

        <table class="table table-bordered table-striped">
            <thead class="table-light">
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Municipio</th>
                    <th>Creado</th>
                    <th>Actualizado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for neighborhood in neighborhoods %}
                    <tr>
                        <td>{{ neighborhood.id }}</td>
                        <td>{{ neighborhood.name }}</td>
                        <td>{{ neighborhood.municipality_name }}</td>
                        <td>{{ neighborhood.created_at|date("Y-m-d H:i") }}</td>
                        <td>{{ neighborhood.updated_at|date("Y-m-d H:i") }}</td>
                        <td>
                            <a href="/neighborhoods/{{ neighborhood.id }}/edit" class="btn btn-sm btn-warning">Editar</a>
                            <a href="/neighborhoods/{{ neighborhood.id }}/delete" class="btn btn-sm btn-danger"
                               onclick="return confirm('¿Eliminar este barrio?')">Eliminar</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6" class="text-center">No hay barrios registrados.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
{% endblock %}